# 39. Combination Sum
**Difficulty**: Medium

**LeetCode Link**: [39. Combination Sum](https://leetcode.com/problems/combination-sum/description/?envType=study-plan-v2&envId=top-interview-150)

---

## Initial Thoughts
.....
---

## Approach 1: 

### Intuition

### Algorithm


### Complexity Analysis
- **Time Complexity**: 
- **Space Complexity**: 

### Code
```c#
public class Solution {
    public IList<IList<int>> CombinationSum(int[] candidates, int target)
    {
        var result = new List<IList<int>>();
        Backtrack(0, target, new List<int>());
        return result;

        void Backtrack(int start, int remaining, List<int> current)
        {
            // Base case (a Valid combination found)
            if (remaining == 0)
            {
                result.Add(new List<int>(current));
                return;
            }
            
            // ✂️ prune
            if(remaining < 0)
                return;

            for (int i = start; i < candidates.Length; i++)
            {
                //add current candidate
                current.Add(candidates[i]);
                
                //explore
                Backtrack(i, remaining - candidates[i], current);
                
                //undo
                current.RemoveAt(current.Count - 1);
            }
        }
    }
}
```
