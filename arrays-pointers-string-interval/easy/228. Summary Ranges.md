# 228. Summary Ranges

**Difficulty**: Medium

**LeetCode Link**: [228. Summary Ranges](https://leetcode.com/problems/summary-ranges/description/?envType=study-plan-v2&envId=top-interview-150)

---

## Initial Thoughts
.....
---

## Approach 1: 

### Intuition

### Algorithm


### Complexity Analysis
- **Time Complexity**: 
- **Space Complexity**: 

### Code
```c#
public class Solution {
    public IList<string> SummaryRanges(int[] nums) {
       var result = new List<string>();
        if (nums.Length == 0) return result;

        int start = nums[0];

        for (int i = 1; i <= nums.Length; i++)
        {
            // Check if we reached the end OR the sequence breaks
            if (i == nums.Length || nums[i] != nums[i - 1] + 1)
            {
                if (start == nums[i - 1])
                {
                    result.Add(start.ToString());
                }
                else
                {
                    result.Add($"{start}->{nums[i - 1]}");
                }

                // Reset start if not at the end
                if (i < nums.Length)
                {
                    start = nums[i];
                }
            }
        }

        return result;
    }
}
```
