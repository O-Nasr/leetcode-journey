# 230. Kth Smallest Element in a BST
**Difficulty**: Medium

**LeetCode Link**: [230. Kth Smallest Element in a BST](https://leetcode.com/problems/kth-smallest-element-in-a-bst/description/?envType=study-plan-v2&envId=top-interview-150)

---

## Initial Thoughts
.....
---

## Approach 1: 

### Intuition

### Algorithm


### Complexity Analysis
- **Time Complexity**: 
- **Space Complexity**: 

### Code
```c#
public class Solution {
    private int kthSmallest = -1;
    private int count = 0;
    public int KthSmallest(TreeNode root, int k) {
        InOrderTraversal(root, k);
        return kthSmallest;
    }

    private void InOrderTraversal(TreeNode root, int k)
    {
        if(root == null || kthSmallest > -1) return;
        InOrderTraversal(root.left, k);
        count++;
        if(count == k) {
            kthSmallest = root.val;
            return;
        }
    
        InOrderTraversal(root.right, k);
    }
}
```
