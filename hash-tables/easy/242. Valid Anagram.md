# 242. Valid Anagram

**Difficulty**: Easy
**LeetCode Link**: https://leetcode.com/problems/ransom-note/submissions/1925587205/?envType=study-plan-v2&envId=top-interview-150
**Pattern**: 
**Date Solved**: [To be filled when solved]

---

## Initial Thoughts

---

## Approach 1: Dictionary

### Intuition

### Algorithm


### Complexity Analysis
- **Time Complexity**: 
- **Space Complexity**: 

### Code
```c#
 public bool IsAnagram(string s, string t) {
    if(s.Length != t.Length) return false;
    
    Dictionary<char, int> map = new Dictionary<char, int>();
    foreach (char c in s)
    {
        if (!map.ContainsKey(c)) map.Add(c, 0);
        map[c]++;
    }
    
    foreach (char c in t)
    {
        if (!map.ContainsKey(c) || map[c] == 0) return false;
        map[c]--;
    }
    
    return true;
}
```

## Approach 2: Array (more efficient)

### Intuition


**Key insight**:

### Algorithm


### Complexity Analysis
- **Time Complexity**: 
- **Space Complexity**: 

### Code
```c#
public bool IsAnagram2(string s, string t)
{
    if (s.Length != t.Length) return false;

    int[] counts = new int[26]; // fixed-size array for 'a' to 'z'
    foreach (char c in s)
    {
        counts[c - 'a']++;
    }

    foreach (char c in t)
    {
        counts[c - 'a']--;
        if (counts[c - 'a'] < 0) return false; 
    }

    return true;
}
```


## Edge Cases & Testing

### Edge Cases Considered


### Test Cases

```

---

