# 290. Word Pattern

**Difficulty**: Easy
**LeetCode Link**: https://leetcode.com/problems/word-pattern/description/?envType=study-plan-v2&envId=top-interview-150
**Pattern**: 
**Date Solved**: [To be filled when solved]

---

## Initial Thoughts


---

## Approach 1: better

### Intuition


### Algorithm


### Complexity Analysis
- **Time Complexity**: 
- **Space Complexity**: 

### Code
```c#
public bool WordPattern(string pattern, string s) {
    Dictionary<char, string> map = new Dictionary<char, string>();
    HashSet<string> hashSet = new HashSet<string>();
    var words = s.Split(' ', StringSplitOptions.RemoveEmptyEntries);
    if(words.Length != pattern.Length) return false;
    for(int i = 0; i < pattern.Length; i++)
    {
        if (!map.ContainsKey(pattern[i]))
        {
            if(hashSet.Contains(words[i])) return false;
            map.Add(pattern[i], words[i]);
            hashSet.Add(words[i]);
        }
        else
        {
            if(map[pattern[i]] != words[i]) return false;
        }
    }
    
    return true;
}
```
---

