# 219. Contains Duplicate II

**Difficulty**: Medium

**LeetCode Link**: [219. Contains Duplicate II](https://leetcode.com/problems/contains-duplicate-ii/description/?envType=study-plan-v2&envId=top-interview-150)

---

## Initial Thoughts
.....
---

## Approach 1: 

### Intuition

### Algorithm


### Complexity Analysis
- **Time Complexity**: 
- **Space Complexity**: 

### Code
```c#
public class Solution {
    public bool ContainsNearbyDuplicate(int[] nums, int k) {
        Dictionary<int, int> map = new Dictionary<int, int>();

        for (int i = 0; i < nums.Length; i++)
        {
            if (map.ContainsKey(nums[i]) && i - map[nums[i]] <= k)
            {
                return true;
            }
            else if (map.ContainsKey(nums[i]) && i - map[nums[i]] > k)
            {
                map[nums[i]] = i;
            }
            else
            {
                map.Add(nums[i], i);
            }
        }
        
        return false;
    }
}
```
