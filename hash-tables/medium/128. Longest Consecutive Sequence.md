# 128. Longest Consecutive Sequence
**Difficulty**: Medium

**LeetCode Link**: [128. Longest Consecutive Sequence](https://leetcode.com/problems/longest-consecutive-sequence/description/?envType=study-plan-v2&envId=top-interview-150)

---

## Initial Thoughts
.....
---

## Approach 1: 

### Intuition

### Algorithm


### Complexity Analysis
- **Time Complexity**: 
- **Space Complexity**: 

### Code
```c#
public class Solution {
    public int LongestConsecutive(int[] nums) {
        if(nums.Length == 0) return 0;
        
        int longestSeq = 0;
        HashSet<int> set = new HashSet<int>(nums);
        int currentNum = 0;
        int currentSeq = 0;
        
        foreach (int num in set)
        {
            if (!set.Contains(num - 1)) // this for get the smallest number (beginning )
            {
                currentNum = num;
                currentSeq = 1;
                while (set.Contains(currentNum + 1))
                {
                    currentNum++;
                    currentSeq++;
                }
                longestSeq = Math.Max(longestSeq, currentSeq);
            }
        }

        return longestSeq;
    }
}
```
